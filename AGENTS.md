# AGENTS.md — قواعد العمل الإلزامية

## المشروع الرئيسي
- ملفات التطبيق الرسمي والوحيد توجد في **جذر هذا المستودع مباشرة**:
  `index.html`, `style.css`, `script.js`, `sw.js`, `manifest.json`, `migration.js`,
  `supabase-client.js`, `supabase-db.js`, `config.js`, `config.example.js`, `icons/`, `libs/`.
- كل التعديلات والإضافات والإصلاحات تتم على هذه الملفات فقط.
- تم نقل المشروع من `pure-app/` إلى الجذر كي يتمكن Vercel من نشره من جذر المستودع
  (يُقرأ `vercel.json` و `index.html` من الجذر مباشرة).

## ممنوع نهائياً
- لا تقرأ، لا تعدّل، لا تنشئ، ولا تستخدم كمصدر للكود أي ملفات داخل:
  - `barakat_account`
  - `electron-app`
  - `web-app`
  - `pure-app` (بقي فارغاً بعد النقل، وهو مستثنى من git)
- لا تنسخ الملفات بين هذه المجلدات وجذر المستودع، ولا تزامن أي تعديلات معها، إلا بطلب صريح من المستخدم.
- الملفات المكررة الموجودة في المجلدات الأخرى تُتجاهل تماماً.

## التشغيل والاختبار والنشر
- التشغيل المحلي: `launcher.ps1` أو `server.ps1` من الجذر.
- الاختبار والبناء والرفع إلى GitHub والنشر على Vercel يتم من جذر المستودع.
- `config.js` يحوي بيانات حقيقية وهو **مستثنى من git**؛ لا يُرفع أبداً.
